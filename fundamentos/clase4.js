var nombre = 'Hector', edad = 20

function imprimirEdad(nombre,edad){
    console.log(`${nombre} tiene ${edad}`) /*Alt+96 (´) */
}
for (let index = 0; index < 3; index++) {
    imprimirEdad("Maria",20)
}